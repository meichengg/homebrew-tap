# Homebrew Tap

Homebrew tap for [Disk Keep Alive](https://github.com/meichengg/disk-keep-alive)

## Installation

```bash
brew install --cask meichengg/tap/disk-keep-alive
```

## Available Casks

- `disk-keep-alive` - Prevent external HDDs from spinning down
